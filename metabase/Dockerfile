FROM metabase/metabase:latest

# Убеждаемся, что Metabase запускается с правильными параметрами
# ENTRYPOINT образа уже настроен, но можно явно указать команду
# Metabase запускается автоматически через ENTRYPOINT образа

