# Следующие шаги после завершения Этапа 2

## ✅ Этап 2 завершен

Модуль метрик создан и готов к переносу в `rely-lead-processor`.

## 📋 Чеклист интеграции

### 1. Найти проект rely-lead-processor

Если проект еще не создан или находится в другом месте, определите его расположение.

### 2. Скопировать модуль

**Вариант A: Использовать скрипт (рекомендуется)**
```bash
cd /Users/rgareev91/Downloads/abc-metrics
./install-metrics-module.sh /path/to/rely-lead-processor
```

**Вариант B: Ручное копирование**
```bash
cp -r metrics-module-template/src/metrics /path/to/rely-lead-processor/src/
```

### 3. Установить зависимости

```bash
cd /path/to/rely-lead-processor
npm install axios puppeteer csv-parse node-cron
npm install --save-dev @types/node-cron
```

### 4. Интегрировать в приложение

Добавить в главный файл приложения (см. `INTEGRATION_EXAMPLES.md`):

```typescript
import metricsModule from './metrics';
app.use('/api/metrics', metricsModule.routes);
metricsModule.scheduler.start();
```

### 5. Настроить переменные окружения

Создать/обновить `.env`:

```env
ABC_METRICS_API_URL=https://abc-metrics.fly.dev
ABC_METRICS_API_KEY=your-api-key
WORKIZ_API_KEY=your-workiz-api-key
WORKIZ_API_SECRET=your-workiz-api-secret
WORKIZ_API_URL=https://api.workiz.com
ELOCAL_USERNAME=help@bostonmasters.com
ELOCAL_PASSWORD=your-password
```

### 6. Протестировать

```bash
# Запустить приложение
npm start

# Проверить endpoints
curl http://localhost:3000/api/metrics/test/workiz/jobs?start_date=2025-01-01
```

## 📚 Документация

- **INTEGRATION_INSTRUCTIONS.md** - Подробные инструкции по интеграции
- **INTEGRATION_EXAMPLES.md** - Примеры интеграции для разных типов приложений
- **MIGRATION_GUIDE.md** - Руководство по миграции
- **metrics-module-template/README.md** - Документация модуля (после копирования)

## 🔍 Проверка

После интеграции проверьте:

- [ ] Модуль скопирован в `src/metrics/`
- [ ] Зависимости установлены
- [ ] Модуль интегрирован в главное приложение
- [ ] Переменные окружения настроены
- [ ] Приложение компилируется без ошибок
- [ ] Endpoints доступны (`/api/metrics/test/workiz/jobs`)
- [ ] Планировщик запускается (логи `[METRICS]`)

## ⚠️ Важно

1. **Изоляция:** Модуль полностью изолирован и не должен конфликтовать с существующим функционалом
2. **API-first:** Все операции с БД идут через API abc-metrics
3. **Идемпотентность:** Все операции UPSERT - можно запускать хоть каждый час
4. **Префиксы:** Все endpoints используют `/api/metrics/*`

## 🚀 После успешной интеграции

1. Удалить функционал синхронизации из `abc-metrics` (оставить только API и БД)
2. Обновить документацию
3. Протестировать полный цикл синхронизации
4. Настроить мониторинг и алерты

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи приложения
2. Проверьте переменные окружения
3. Проверьте доступность API abc-metrics
4. См. раздел "Устранение проблем" в `INTEGRATION_INSTRUCTIONS.md`



